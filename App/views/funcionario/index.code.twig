{% extends view('app') %}

{% block content %}

        <div class="row">
            <div class="col-md-12">
                <div class="panel panel-primary">
                    <div class="panel-heading">
                        <h3 class="panel-title">Periféricos <a href="funcionario/novo" class="fa fa-plus pull-right"></a></h3>
                    </div>
                    <div class="panel-body">
                        <table class="table">
                            <thead>
                            <tr>
                                <th>ID</th>
                                <th>Nome</th>
                                <th>Email</th>
                                <th>Telefone</th>
                                <th>Ramal</th>
                                <th>#</th>
                            </tr>
                            </thead>
                            <tbody>
                            {% for  funcionario in results %}
                                <tr>
                                    <td>{{ funcionario.nome }}</td>
                                    <td>{{ funcionario.email }}</td>
                                    <td>{{ funcionario.telefone }}</td>
                                    <td>{{ funcionario.ramal }}</td>
                                    <td>{{ funcionario.setor_id }}</td>
                                    <td>
                                        <a href="funcionario/editar" class="fa fa-edit text-info"></a>
                                        <a href="edit.html.twig" class="fa fa-trash text-danger"></a>
                                    </td>
                                </tr>
                            {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>

{% endblock %}