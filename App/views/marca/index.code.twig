{% extends view('app') %}

{% block content %}

        <div class="row">
            <div class="col-md-12">
                <div class="panel panel-primary">
                    <div class="panel-heading">
                        <h3 class="panel-title">Marcas <a href="/marca/novo" class="fa fa-plus pull-right"></a></h3>
                    </div>
                    <div class="panel-body">
                        <table class="table">
                            <thead>
                            <tr>
                                <th>ID</th>
                                <th>Nome</th>
                                <th>#</th>
                            </tr>
                            </thead>
                            <tbody>
                            {% for  marca in results %}
                                <tr>
                                    <td>{{ marca.id }}</td>
                                    <td>{{ marca.nome }}</td>
                                    <td>
                                        <a href="marca/editar?id={{ marca.id }}"  class="fa fa-edit text-info"    ></a>
                                        <a href="marca/deletar?id={{ marca.id }}" class="fa fa-trash text-danger" ></a>
                                    </td>
                                </tr>
                            {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>

{% endblock %}