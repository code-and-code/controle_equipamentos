{% extends view('app') %}

{% block content %}

    <div class="row">
        <div class="col-md-12">
            <div class="panel panel-primary">
                <div class="panel-heading">
                    <h3 class="panel-title">Serial | Editar <a href="/compotador/por_serial?id={{ licenca.id}}" class="fa fa-user pull-right"></a></h3>
                </div>
                <div class="panel-body">
                    <form role="form" action="/licenca/serial/atualizar" method="post">

                        <input type="hidden" name="id" value="{{ serial.id }}">
                        <div class="form-group">
                            <label class="control-label" for="2">
                                Número
                            </label>
                            <input class="form-control" id="2" placeholder="" name="numero" type="text" value="{{ serial.numero }}">
                        </div>
                        <div class="form-group">
                            <label class="control-label">
                                Data Vencimento
                            </label>
                            <input class="form-control" placeholder="" name="data_vencimento" type="text" value="{{ serial.data_vencimento }}">
                        </div>
                        <button type="submit" class="btn btn-primary">
                            Gravar
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>
{% endblock %}