{% extends view('app') %}

{% block content %}

    <div class="row">
        <div class="col-md-12">
            <div class="panel panel-primary">
                <div class="panel-heading">
                    <h3 class="panel-title">Local | Editar <a href="/patrimonio/por_local?id={{ local.id}}" class="fa fa-user pull-right"></a></h3>
                </div>
                <div class="panel-body">
                    <form role="form" action="/local/atualizar" method="post">

                        <input type="hidden" name="id" value="{{ local.id }}">
                        <div class="form-group">
                            <label class="control-label" for="2">
                                Nome
                            </label>
                            <input required class="form-control" id="2" placeholder="" name="nome" type="text" value="{{ local.nome }}">
                        </div>
                        <button type="submit" class="btn btn-primary">
                            Gravar
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>
{% endblock %}